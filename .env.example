# Copiar a .env y completar. .env está en .gitignore.

# Apify — Console → Settings → Integrations → API token
APIFY_TOKEN=

# Anthropic — necesario recién en la Fase 1 (el CLI).
# En la Fase 0 el modelo es el propio Claude Code / Claude Desktop, sin API key.
ANTHROPIC_API_KEY=
